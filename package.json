{
  "name": "jwtmock",
  "version": "1.0.0",
  "description": "Mock Jwt Protected Endpoints",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch ./db.json",
    "start-auth": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-data": "node generateData.js > db.json"
  },
  "author": "akashneo3@gmail.com",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "faker": "^4.1.0",
    "json-server": "^0.14.2",
    "jsonwebtoken": "^8.4.0"
  }
}
